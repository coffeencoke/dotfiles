Place Ruby unit tests here.

By default, this package is configured to use RSpec: https://relishapp.com/rspec

You can run all tests with:
brazil-build test
Run the tests in a specific file with:
brazil-build test SPEC=test/your_spec_file.rb
Or run one specific test with:
brazil-build test SPEC_OPTS="-e \"should work when things happen\""