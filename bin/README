Place Ruby scripts here.

Start scripts with this shebang line:
#!/apollo/sbin/envroot "$ENVROOT/bin/ruby"

Run scripts in your working directory like this:
brazil-runtime-exec ruby bin/my-script

Run scripts in your workspace build artifacts like this:
brazil-runtime-exec my-script